# SK/EU production (Hetzner + Coolify). Shop/Next stays on Vercel.
# Postgres and Redis are Coolify Database resources — not this file.
# TLS is the Coolify proxy (not in-stack Caddy).
#
# Coolify lists a UI row for every `environment:` key per service, plus a
# shared copy for interpolation from this file. Keep this file to the SK contract
# (see .env.coolify.example). Do not paste .env.prod.example or .env.example.
#
# Coolify: Docker Compose build pack, this file, Base Directory
#   green-angels-backend. Mark service `api` as the public one.
# Public URL: https://api.<host> (port 443). Container port is 3001.
# Coolify Domain UI may use a :3001 suffix as a *backend* port hint only —
# do not put :3001 on API_PUBLIC_URL, Vercel NEXT_PUBLIC_API_URL, or webhooks.
#
# Startup: migrate (`prisma migrate deploy`, one-shot) → api.
# Start Coolify Postgres + Redis first. Failed migrate → api does not start.
# UA/HostPro: use docker-compose.prod.yml instead (in-stack PG/Redis AOF).
#
# migrate receives DATABASE_URL only. UA payment/delivery/SMS env is not
# interpolated here (Nest still reads them if someone injects later; SK
# does not need Coolify rows for Mono / Nova Poshta / TurboSMS).

services:
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrate
    restart: 'no'
    command: ['npx', '--no-install', 'prisma', 'migrate', 'deploy']
    environment:
      DATABASE_URL: ${DATABASE_URL}

  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    restart: unless-stopped
    expose:
      - '3001'
    environment:
      # Dockerfile already sets these; literals here so Show Compose is explicit.
      NODE_ENV: production
      PORT: 3001
      MEDIA_DRIVER: r2
      ENABLE_PHOTOS_DEBUG: '0'
      DATABASE_URL: ${DATABASE_URL}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN}
      API_PUBLIC_URL: ${API_PUBLIC_URL}
      SHOP_PUBLIC_URL: ${SHOP_PUBLIC_URL}
      R2_ENDPOINT: ${R2_ENDPOINT}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
      R2_BUCKET: ${R2_BUCKET}
      R2_PUBLIC_BASE_URL: ${R2_PUBLIC_BASE_URL}
      FLEXI_SECRETS_KEY: ${FLEXI_SECRETS_KEY:-}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      SMTP_FROM: ${SMTP_FROM:-}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY:-}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET:-}
      PHOTO_API_KEYS: ${PHOTO_API_KEYS:-}
      LEGACY_PHOTO_API_KEY: ${LEGACY_PHOTO_API_KEY:-}
      VIBER_BOT_TOKEN: ${VIBER_BOT_TOKEN:-}
      VIBER_USER_IDS: ${VIBER_USER_IDS:-}
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:3001/health']
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 40s
